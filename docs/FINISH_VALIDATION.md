# Finish validation — 2026-09-06

## Current Multicam Tropic, stripper, and runway run

DayZ Tools packaging and DayZ 1.29 diagnostic-server compilation passed.
The updated sandbox template was executed with temporary test calls injected
after fixture creation. Final result: **619 PASS, 0 FAIL**, with the
`RUNWAY AND LOADOUT COMPLETE` marker and automatic server shutdown.

- Five paint finishes passed application and restoration checks on M4A1, AKM,
  and Mag_CMAG_30Rnd_Black, including the new Multicam Tropic PAA.
- All five paint cans reject Strip Paint. The dedicated stripper accepts only
  painted compatible targets and rejects insufficient quantity.
- Server completion rejects a paint can, strips with a stripper, consumes 10
  quantity on success, and consumes nothing on a stale second strip.
- The runway anchor `4475 0 10200` and all 196 dynamically generated weapon
  and magazine fixture positions returned `cp_concrete2` from SurfaceGetType.
- A test player equipped through StartingEquipSetup received a backpack and
  exactly two full cans of each of the six types. The backpack alone was too
  small, so empty vest/leg slots now receive additional cargo clothing.

The vanilla diagnostic plugin warning and weapon FSM errors from enumerating
all public weapon configs were present during fixture creation. They did not
prevent the tests completing; no PaintZ compilation errors occurred.

Client visuals, connected-client loadout presentation, and multiplayer paint
replication remain manual acceptance checks. Can labels are generated by
paintzgen and exported to `data/cans`; finish-rendered coating textures for painted
surfaces are exported separately to `data/surfaces`.

## Earlier four-finish run

The results below describe the earlier shared-stripper behavior. The subsequent
Multicam Tropic and dedicated-stripper changes supersede that action policy.

DayZ Tools AddonBuilder packaged the updated mod successfully. An isolated
DayZ 1.29 diagnostic server compiled its World scripts and the opt-in
`tools/sandbox/PaintZ_FinishSmokeTest.c` mission test, then exited automatically.
The final run logged **213 PASS, 0 FAIL**, followed by the COMPLETE marker.

Validated server-side:

- All four cans spawn with their intended finish IDs.
- M4A1, AKM, and Mag_CMAG_30Rnd_Black accept every finish on the dynamically selected surface.
- Untouched items reject Strip Paint with all four cans.
- Painted items accept Strip Paint with all four cans, including a different finish's can.
- Stripped items reject Strip Paint again and recover their initial texture.
- Classname and health remain unchanged; magazine ammo count remains unchanged.
- Null targets and negative selection indices have no paint to strip.
- SCARH (stock SCR17) is safely rejected: `camo1` and `camo2` are ambiguous under the existing heuristic.
- Mag_STANAG_30Rnd is safely rejected because it has no hidden selections.

No target compatibility logic was changed. Fixture classnames appear only in
tests. No item replacement or persistence hooks were added.

The diagnostic server emitted its existing vanilla PluginConfigDebugProfile
registration stack trace during startup. It did not prevent mission execution;
there were no PaintZ compilation errors or failed smoke assertions.

Still unverified: client action-menu rendering, two-player strip races,
multiplayer/late-relevancy magazine replication, attachment and chamber
preservation in live play, and visual pattern scale/material response. FDE is
a darker, less yellow approximation toward the SCR17 reference, not a sampled
color match. See `RELEASE_ACCEPTANCE.md` for the full acceptance checklist.

Woodland can artwork uses `data/cans/pz_c_wdl_co.paa`; painted surfaces use
the finish-rendered pattern at `data/surfaces/pz_c_wdl_co.paa`.

## Finish-change guard and dropped placement

Final DayZ 1.29 diagnostic run completed without failed smoke assertions.
Changing from FDE to Black was blocked in both action conditions and server
completion, without paint consumption, on a weapon and magazine. Stripping
then enabled the new finish.

The final placement helper initializes the object's transform before calling
PlaceOnSurfaceRotated. M4A1, AKM, CMAG, and AKM magazine samples received their
native resting rotation (approximately +/-90 degrees pitch) and model-specific
surface heights while retaining the intended runway coordinates. Native drop
position checks passed. Client visual inspection remains pending.

Transparent coats and mixing were not implemented; see PAINT_COATS_FEASIBILITY.md.

The finish-change restriction was subsequently reverted at the user's request. Its assertions above describe the temporary implementation, not current behavior. Only the placement fix remains; paint can overwrite an existing finish.
